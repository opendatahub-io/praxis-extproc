apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# STREAMED — incremental body processing, trailers still sent.
resources:
  - ../base

patches:
  - target:
      kind: EnvoyFilter
      name: praxis-extproc-ext-proc
    path: mode-patch.yaml
