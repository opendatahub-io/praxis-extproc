apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# BUFFERED — mirrors the odh production overlay.
resources:
  - ../base

patches:
  - target:
      kind: EnvoyFilter
      name: praxis-extproc-ext-proc
    path: mode-patch.yaml
