apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Shared ext-proc HTTP filters (FDS defaults). The fds/, buffered/, and
# streamed/ overlays consume this base and patch the body/trailer modes.
resources:
  - ext-proc.yaml
